apiVersion: api-management.sensedia.com/v1
kind: Plan
spec:
  name: '[PRD][CLI] Default Plan'
  apiRef:
    name: cli-swapi-poc
    version: 1.0.0
  default: true
  flows:
  - context:
      revision: all
      resourceRef:
        name: all
      operationRef:
        method: all
        path: /
    requestInterceptors:
    - restrictAccess:
        environmentRefs:
        - name: "[PRD] Sensedia CLI"
        type: ALLOW_LIST
