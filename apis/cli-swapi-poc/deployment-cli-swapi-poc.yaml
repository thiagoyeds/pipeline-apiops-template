apiVersion: api-management.sensedia.com/v1
kind: Deployment
spec:
  apiRef:
     name: cli-swapi-poc
     version: 1.0.0
  environmentRef:
     name: "[HML] Sensedia CLI"
  # Other fields
  revision: last
