apiVersion: api-management.sensedia.com/v1
kind: Plan
spec:
  name: '[HML] API Ops Days Plan Sample'
  apiRef:
    name: APIOps API Days API Sample
    version: v1
  default: true
  flows:
  - context:
      revision: all
      resourceRef:
        name: all
      operationRef:
        method: all
        path: /
    requestInterceptors:
    - restrictAccess:
        environmentRefs:
        - name: "[HML] APIOps API Days Env Sample"
        type: ALLOW_LIST
